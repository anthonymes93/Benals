# Resend (https://resend.com) powers the /api/contact serverless function.
# These are server-side only — never prefix them with VITE_, which would
# bundle them into browser-visible code.
RESEND_API_KEY=re_example
CONTACT_TO_EMAIL=owner@example.com
CONTACT_FROM_EMAIL=Benals Website <website@example.com>
